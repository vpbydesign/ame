name: ame_flutter_ui
description: AME Flutter renderer — Material 3 widgets for AmeNode trees
version: 1.3.0
homepage: https://github.com/anthropics/ame-spec

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  ame_flutter:
    path: ../ame-flutter
  cached_network_image: ^3.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
